[tool.poetry]
name = "42-project"
version = "2.0.0"
authors = ["loboehm"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.1.0"
requests = "^2.31.0"
matplotlib = "^3.7.2"
numpy = "^1.26.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
